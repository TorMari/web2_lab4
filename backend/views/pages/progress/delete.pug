extends ../../layout

block content
   h2(id='title') Delete the project in progress
   div(class='container' id='cont')
      form(id='progress-delete-form' name='progress-delete-form' class='form-horizontal' method="POST" action='')
         input(id='id' type="hidden" name='id' value=progress.id class='form-control')
         div(class='form-group')
         div(class='col-md-12') 
            h5 Do you really want to delete project in progress  #{progress.id}?
         div(class='form-group')
         div(class='col-md-12')
            a(href=`/progress/list` class="btn btn-success" id = 'del_btn') No
            button(type='submit' class='btn btn-danger' id= 'del_btn') Delete